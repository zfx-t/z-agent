/**
 * @z-agent/ai — LLM protocol package (scaffold).
 *
 * Will own: Message types, assistant stream events, StreamFn shape,
 * faux provider, OpenAI Responses API streaming adapter.
 */

export const AI_PACKAGE = "@z-agent/ai" as const;
